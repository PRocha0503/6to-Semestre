import { ItemPredicate } from "@blueprintjs/select"


// const itemPredicate = <T, K extends string>(key: K) => (query: string, item: T, index?: number, exactMatch?: boolean) => {
//     return (query: string, item: T, index?: number, exactMatch?: boolean) => {
//         return (item[key]).toLowerCase().indexOf(query.toLowerCase()) >= 0
//     }
// }